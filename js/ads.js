/* ============================================================
   ADS — AdSense-ready placeholder zones
   ============================================================ */

export function initAds() {
    // Ad zones are already in the HTML
    // This module is ready for Google AdSense code injection
    // When ready, replace the placeholder content with actual ad code

    // Example:
    // const adSlots = document.querySelectorAll('[data-ad-slot]');
    // adSlots.forEach(slot => {
    //   const ins = document.createElement('ins');
    //   ins.className = 'adsbygoogle';
    //   ins.style.display = 'block';
    //   ins.setAttribute('data-ad-client', 'ca-pub-XXXXXXXX');
    //   ins.setAttribute('data-ad-slot', slot.dataset.adSlot);
    //   ins.setAttribute('data-ad-format', 'auto');
    //   slot.innerHTML = '';
    //   slot.appendChild(ins);
    //   (adsbygoogle = window.adsbygoogle || []).push({});
    // });
}
